'use strict'
/**
 * 页面加载完成后默认执行的方法，用于给页面初始化添加效果等
 * create by wangxd
 */
var dseDefine = dseDefine === null ? define : dseDefine;
dseDefine('dseLoad',['dseDep'],function(require,exports,module){
	let DseDep = require('dseDep').DseDep,
		$ = DseDep.$;

	$(function(){
	})
});